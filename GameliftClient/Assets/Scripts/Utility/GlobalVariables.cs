using System;
using System.Collections;

public static class GlobalVariables
{
    //Time Span
    public readonly static TimeSpan wait1sec = TimeSpan.FromSeconds(1); 
    public readonly static TimeSpan wait3sec = TimeSpan.FromSeconds(3);
    public readonly static TimeSpan wait5sec = TimeSpan.FromSeconds(5);
}